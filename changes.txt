introducing a new feature on the app - Handle_Raw_COT
The app will be working hand in hand with the app - Get_Data

So here is the idea - the models on the Get_Data app have stored urls of all historical cot reports that I need for Currencies, bitcoin and metals.
I need a functionality that parses through the links one by one, takes note of that url, opens it and scraps the data necessary(just as when importing the html files of the existing code and functionalities) and then saves the data on the database through a new model class (the only difference is just the naming of the class), but in every other form, it saves the data as the current models.

Basically the idea is that I want to scrap data off of the cftc website with the reference of the stored report urls. It should be done at a click of a button. The button should be located on scraping_data.html.

Tell me which scrapping libraries I need to install to make it easier for you to scrap the data